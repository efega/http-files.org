---
title: CLI Tools
description: Command-line tools for running .http files in CI/CD and automation.
---

## JetBrains ijhttp

{% client-card id="ijhttp" /%}

The official JetBrains CLI for running `.http` files. Requires JDK 21+. Available as a Docker image: `jetbrains/intellij-http-client`.

Supports the full JetBrains HTTP Client feature set including pre-request scripts, response handlers, environments, and the Crypto API.

## httpyac CLI

{% client-card id="httpyac-cli" /%}

Install via npm: `npm install -g httpyac`. Supports `--json` and `--junit` output formats, `--bail` for fail-fast, `--repeat` with parallel mode.

The same broad compatibility as the httpyac VS Code extension — supports both JetBrains and REST Client syntax.

## httprunner

{% client-card id="httprunner" /%}

Rust-based CLI with TUI and GUI modes. Supports both JetBrains and REST Client syntax, `http-client.env.json`, and HTML/Markdown report generation.

## Resterm

{% client-card id="resterm" /%}

Terminal API client for `.http` and `.rest` files with support for HTTP, GraphQL, gRPC, WebSockets, SSE, workflows, SSH tunnels, profiling, OpenAPI, Kubernetes port-forwarding, and headless execution.

## Kulala CLI

{% client-card id="kulala-cli" /%}

CLI companion to kulala.nvim. Requires Neovim and curl. GitHub Action available for CI pipelines.

## httprun

{% client-card id="httprun" /%}

CLI targeting VS Code REST Client compatibility. Supports batch execution of `.http` files.

## dot-http

{% client-card id="dot-http" /%}

Rust CLI with companion vim plugin.

## Assay CLI

{% client-card id="assay-cli" /%}

Run with `npx assay-http`, or install via npm: `npm install -g assay-http`. Point it at a file or a directory; every request runs in order, sharing the chain store and the cookie session, so a login at the top of a file authenticates the requests below it.

Emits JUnit XML with one `<testcase>` per assertion rather than per request, plus JSON. Exits non-zero when an assertion fails. Values from `http-client.private.env.json` are masked in the output by default. No configuration file and no runtime dependencies. A GitHub Action is available.
